export type Settings = {
    prefix: string,
    logChannel: string,
    verifyRole: string,
    jailRole: string,
    modRoles: string[],
    logFlags: number,
    badWords: string[],
    badNames: string[][],
    antiSpam: boolean
};